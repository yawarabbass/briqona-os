/*
|--------------------------------------------------------------------------
| BRIQONA OS - Application Provider
|--------------------------------------------------------------------------
|
| Global application provider.
| Future providers will be connected here.
|
*/

export default function AppProvider({ children }) {
  return children;
}
